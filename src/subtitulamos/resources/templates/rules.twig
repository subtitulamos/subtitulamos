{% extends "base.twig" %}
{% import 'macros/assets.twig' as assets %}

{% block title %}Normas del sitio{% endblock %}
{% block css %}
  {{ assets.css('rules') }}
{% endblock %}

{% block content %}
  <div class="content limited-width">
    <h1>Normas del sitio</h1>
    <div id="rules-navigation-list" class="navigation-list no-scroll-bar">
      <a href="/rules">
        <div class="navigation-item {% if rules_type == 'main' %}selected{% endif %}">TRADUCCIÓN</div>
      </a>
      <a href="/rules/community">
        <div class="navigation-item {% if rules_type == 'community' %}selected{% endif %}">COMUNIDAD</div>
      </a>
      <a href="/rules/upload">
        <div class="navigation-item  {% if rules_type == 'upload' %}selected{% endif %}">SUBIDAS</div>
      </a>
    </div>

    <div id="rules-container">
      {% include "rules/rules_" ~ rules_type ~ ".twig" %}
    </div>
  </div>
{% endblock %}

{% block scripts %}
  {{ assets.js('rules') }}
{% endblock %}
